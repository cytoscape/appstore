This folder contains the MaxMind GeoLite database files. GeoLite's homepage is:
  http://dev.maxmind.com/geoip/geolite
These database files are used by MaxMind's GeoIP library to convert IP
addresses to geographical locations.

To obtain the latest data from MaxMind, enter:
  make clean all
This will download the MaxMind database files.

The 'last_updated' file contains the date and time of when the latest database
files were downloaded.
